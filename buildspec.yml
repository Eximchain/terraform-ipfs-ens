version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 10
  build:
    commands:
      - cd build
      - zip -r ../ipfs-deployable.zip *
      - cd ..
artifacts:
  name: BUILD
  files:
    - ipfs-deployable.zip